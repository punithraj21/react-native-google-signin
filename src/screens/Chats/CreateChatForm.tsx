import React from "react";
import { Text } from "react-native";
import { createNewThread } from "../../utils/createNewChat";

const CreateChatForm = () => {
  
  return <Text>CreateChatForm</Text>;
};

export default CreateChatForm;
